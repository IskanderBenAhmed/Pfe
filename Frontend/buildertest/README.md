
```
yarn install
```


```
yarn run serve
```


```
yarn run build
```

```
yarn run lint
```

```
uses Node 14.17.5 
simply install nvm w lancy nvm install 14.17.5 (recommanded powershell)
then 
nvm List to show list of Node versions installed

nvm use 14.17.5 bech tactivity el version
reload vscode to apply the changes then launch

waiting to refactor the code to work on latest node version ( currently 20.3.0)
```